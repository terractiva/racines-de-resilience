const actionFontSize = 0.004; // Taille du texte des actions en % de la hauteur de l'arrière plan
const nbFilterResultsShowed = 10;
const zoomMax = 5;
const zoomMin = 1;

export { actionFontSize, nbFilterResultsShowed, zoomMax, zoomMin };
